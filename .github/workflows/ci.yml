name: CI - Build & Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  issues: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: 📦 Install frontend dependencies
        working-directory: frontend
        run: npm ci 

      - name: 🏗️ Build frontend
        working-directory: frontend
        run: npm run build
      
      - name: 📦 Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: 🧪 Run backend tests
        working-directory: backend
        run: npm test

      - name: Create issue on failure
        if: failure() && github.ref == 'refs/heads/main'
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'CI failed on main',
              body: `Run: ${context.runId}`
            })

      # Keep the build around so you can download it from Actions tab if something fails
      - name: 📤 Upload frontend build artifact
        if: always() 
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist
          retention-days: 2
  deploy:
      runs-on: ubuntu-latest
      needs: build
      #  This is the key line!
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  
      steps:
        - name: ⬇️ Download frontend build artifact
          uses: actions/download-artifact@v4
          with:
            name: frontend-build
            path: frontend-dist
  
        - name: 📤 Copy frontend dist to server
          uses: appleboy/scp-action@v0.1.4
          with:
            host: ${{ secrets.SERVER_HOST }}
            username: ${{ secrets.SERVER_USER }}
            key: ${{ secrets.SSH_PRIVATE_KEY }}
            source: "frontend-dist/*"
            target: "${{ secrets.SERVER_PATH }}/frontend/dist"
            strip_components: 1
            
        - name: 🚀 Deploy to server
          uses: appleboy/ssh-action@v1.0.3
          with:
            host: ${{ secrets.SERVER_HOST }}
            username: ${{ secrets.SERVER_USER }}
            key: ${{ secrets.SSH_PRIVATE_KEY }}
            script: |
              #!/bin/bash
              set -e  # Exit on error
              set -x  # Trace commands (for debug; remove later)
              
              # Source profiles for non-interactive shell
              [ -f ~/.bashrc ] && source ~/.bashrc
              [ -f ~/.profile ] && source ~/.profile
              
              echo "Deploy attempt at $(date)" >> ~/deploy_debug.log
              
              # Explicitly set the path to include common Node/NVM locations
              export NVM_DIR="$HOME/.nvm"
              
              # Load NVM (Force it)
              if [ -f "$NVM_DIR/nvm.sh" ]; then
                  source "$NVM_DIR/nvm.sh"
              else
                  echo "Critical Error: nvm.sh not found at $NVM_DIR/nvm.sh" | tee -a ~/deploy_debug.log
                  exit 1
              fi
              
              # Use NVM to set the path
              nvm use 22 || nvm install 22
              
              # Hardcode Node path as fallback (uses your exact version)
              export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"
              
              # Confirm environment before moving forward
              echo "Using Node: $(node -v)" | tee -a ~/deploy_debug.log
              echo "Using Path: $PATH" | tee -a ~/deploy_debug.log
              
              # Navigate and execute
              cd "${{ secrets.SERVER_PATH }}" || { echo "Directory not found" | tee -a ~/deploy_debug.log; exit 1; }
              
              echo "Starting Git Reset..." | tee -a ~/deploy_debug.log
              git fetch origin
              git reset --hard origin/main
              
              echo "Installing dependencies..." | tee -a ~/deploy_debug.log
              cd backend
              npm ci --omit=dev
              
              echo "Restarting PM2..." | tee -a ~/deploy_debug.log
              pm2 restart myapp  # Adjust to your app name; or use 'pm2 reload myapp' for cluster mode
